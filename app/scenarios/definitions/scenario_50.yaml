id: 50
name: "JSON processed to textarea"
description: "Payload as part of a JSON blob processed by JS into <textarea>"
category: "Common HTML"
template_block: "textarea_json_processed_block"
html_snippet: |
  <textarea id="output"></textarea>
js_snippet: |
  let data = {"user_input": "{{ payload }}"};
  document.getElementById('output').value = JSON.stringify(data);

